% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_sliding_centroids.R
\name{build_sliding_centroids}
\alias{build_sliding_centroids}
\title{Sliding-Window Centroids in a Fixed LSA Space}
\usage{
build_sliding_centroids(
  dk,
  time_index,
  window_size = 10,
  anchor = c("end", "center"),
  verbose = TRUE
)
}
\arguments{
\item{dk}{Numeric matrix (n_docs x k) of reduced-space document vectors (e.g., \code{lsa_result$dk} rows).}

\item{time_index}{Numeric or integer vector of length \code{nrow(dk)}, used for labeling windows by time.}

\item{window_size}{Integer window size (e.g., 10). Default: 10.}

\item{anchor}{Either "end" (use window end row's time) or "center" (use median row's time). Default: "end".}

\item{verbose}{Logical; messages. Default: TRUE.}
}
\value{
A list with:
\describe{
\item{centroids}{Matrix (n_windows x k) of window centroids.}
\item{summary}{Data frame with \code{window_id}, \code{start_row}, \code{end_row}, \code{anchor_time}.}
}
}
\description{
Compute centroids over a moving window of utterances using the reduced-space
document matrix (rows = utterances, columns = LSA dimensions).
}
\examples{
\dontrun{
out <- build_sliding_centroids(dk, time_index = df$X, window_size = 10)
}
}
