% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_ks.R
\name{run_ks}
\alias{run_ks}
\title{Run Knowledge Sharing Analysis}
\usage{
run_ks(
  df,
  participant_col = "participant",
  group_col = NULL,
  text_col = "text",
  time_col = "X",
  k = 100,
  window_size = 10,
  anchor = c("end", "center"),
  verbose = TRUE
)
}
\arguments{
\item{df}{A data frame with at least participant, text, and time columns.}

\item{participant_col}{Column name for participant IDs. Default: "participant".}

\item{group_col}{Optional column name for grouping (e.g., team). Default: NULL.}

\item{text_col}{Column name of text. Default: "text".}

\item{time_col}{Column indicating temporal order (integer index or timestamp). Default: "X".}

\item{k}{LSA dimensions. Default: 100.}

\item{window_size}{Integer window length (utterances). Default: 10.}

\item{anchor}{"end" or "center" for window anchor times. Default: "end".}

\item{verbose}{Logical; messages. Default: TRUE.}
}
\value{
A named list keyed by group name (or "overall" if no group), where each element contains:
\describe{
\item{centroids}{Matrix of window centroids (n_windows x k).}
\item{summary}{Data frame with window metadata and change metrics.}
\item{visualizations}{List of ggplots from \code{\link[=visualize_ks_plot]{visualize_ks_plot()}}.}
\item{df}{Group data with reduced vectors appended.}
}
}
\description{
Compute sliding-window centroids over a conversation and quantify how the
centroid changes across windows (cosine similarity and Euclidean delta).
Can operate overall or per group.
}
\examples{
\dontrun{
out <- run_ks(cleaned_df, group_col = NULL, time_col = "X", window_size = 10)
out$overall$visualizations$cosine_plot
}

}
